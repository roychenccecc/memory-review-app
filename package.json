{
  "name": "memory-review-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html app.js app.tag-selector-tree.js app.force-schedule-migration.js styles.css dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
